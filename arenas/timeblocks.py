from datetime import timedelta

# All reservation blocks (minutes)
BLOCK_DELTAS = [
    timedelta(minutes=90),  # 1h30
    timedelta(minutes=90),
    timedelta(minutes=90),
    timedelta(minutes=90),
]
